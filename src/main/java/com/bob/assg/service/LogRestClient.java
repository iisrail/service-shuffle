/*
 * package com.bob.assg.service;
 * 
 * import org.springframework.cloud.openfeign.FeignClient; import
 * org.springframework.web.bind.annotation.PostMapping; import
 * org.springframework.web.bind.annotation.RequestBody;
 * 
 * import com.bob.assg.web.LogDto;
 * 
 * @FeignClient(name = "log-service", url = "${feign.url}") public interface
 * LogRestClient {
 * 
 * @PostMapping( value = "/log/") public void log(@RequestBody LogDto logDto);
 * 
 * }
 */
